# Candidate invariant and observed-failure matrix

This matrix records the enforcement state before the frozen-candidate
evaluation. "Return Desk" means the non-admissive Python and browser consistency
inspectors. A hash match establishes the bytes inspected, not their origin,
truth, execution history, or download-button identity.

| Invariant or observed failure | Deterministic code | Tests | Return Desk | Model instructions | Current disposition |
| --- | --- | --- | --- | --- | --- |
| Every declared artifact exists and its current final bytes match the declared SHA-256 and size | `provenance.verify_local_artifact`; evaluation-bundle checker | Return Desk hash/missing/mutation tests; bundle-checker tests | Recomputes bounded local hashes and sizes; mismatch blocks; missing remains review-needed | Requires post-write reread, hash, size, freeze, then return serialization | Enforced for the inspected snapshot. Write order and historical immutability are not inferable from a matching snapshot alone. |
| Artifact generation is one acyclic deterministic transaction | The complete `gpt_artifact_compiler.py` is embedded verbatim in `BSC_EXECUTION_AND_RECEIPTS.md` and executed in Data Analysis; it canonicalizes all eight execution rows and the report projection, freezes supplied bytes, derives identities and the runtime ledger, validates topology, and serializes `audit_return.json` last | Compiler roster, execution-status, transaction-order, post-freeze mutation, and final-byte contract checks | Checks the supplied final snapshot and rejects broken bindings; it does not infer an unrecorded earlier write | Execute the embedded compiler; never rewrite it or ask model prose to reproduce hashes, byte counts, Base64, runtime strings, or an independent execution ledger | The runtime-reachable compiler is the production finalizer. A forbidden unexecuted status or contradictory binding cannot survive into final bytes. |
| Export transport matches the compiler-finalized artifact | Compiler v9 derives one deterministic bounded multi-artifact container from the finalized non-source outputs plus `audit_return.json` after the return is serialized. Same-response transport v2 zlib-compresses the container into contiguous data shards of at most 2,048 bytes and adds one `xor_parity_v1` shard over zero-padded data shards. Controller-record v5 binds the complete raw response, extracts the one compiler block without reserialization, validates the exact sorted roster, framing, bounds, identities, and topology, and reconstructs locally without a later `/mnt/data` path. It may recover exactly one content-faulted data shard only with intact metadata and expected ASCII Base64 text length and valid remaining data plus parity, then reruns every aggregate, container, member, and topology check. It binds explicit per-file direct-attempt outcomes, deterministically derives `artifact_transport.json` from observations and selected bytes, and binds the deterministic recovery receipt in `controller_record.json` | Same-transaction determinism, exact expected non-source roster, known `independently` bytes, one recoverable content fault, exact-length invalid Base64, hash mutation, valid data with degraded parity, aligned-quartet omission, metadata mutation, multiple bad data, bad data plus bad parity, missing/extra/duplicate/reordered member or shard, unsafe path, size/hash/Base64/compression mutation, stdout bound, terminal-LF, raw/parser mutation, response omission, missing/forged direct-attempt observation, direct-only extra output, direct/reconstructed mismatch, and local-byte-mismatch tests | Not part of envelope semantics | Direct acquisition remains primary. Append the compiler-v9 complete canonical stdout byte-for-byte in one final fenced code block during the original response; never regenerate, trim, or manually reproduce its runtime, hashes, sizes, Base64, data shards, or parity | Bundle integrity is always validated. Recovery is limited to the one exact data-content fault; the unrecoverable cases fail closed. Valid data with only exact-length bad parity records `parity_degraded_not_used`. A direct copy is selected when available, the reconstructed member is fallback-only, and both must match byte-for-byte when present. Missing bytes alone never establish `no_download_event`. The bundle identifies only the exported container received by the controller. A completed response that omits or unrecoverably mutates it is `candidate_failed`; controller loss or mutation of an otherwise present block is `trial_invalid_controller`. If original download-button bytes are unavailable, identity remains `transport_identity_unresolved`; neither download-byte identity nor corruption is established. |
| Candidate result, controller validity, and transport observability are distinct outcome axes | Evaluation controller classifies controller validity before candidate scoring and records transport observability separately | Synthetic complete/incomplete roster, parser mutation, contradiction, and unavailable-byte cases | Runs only after the expected roster is complete; a mutated replay is invalid | A substantive contradiction remains `candidate_failed`; controller omission is `trial_invalid_controller`; unavailable original bytes are `transport_identity_unresolved` | No controller or transport classification may rescue a genuinely contradictory candidate, and an invalid controller trial is neither a candidate pass nor failure. |
| Scientific-verdict applicability is explicit and cannot be inferred from a missing oracle | Generated cases carry `research_projection_requirement`; the bundle and suite checkers recompute the v2 score contract | Scientific empty/disallowed/exact-mismatch negatives; status-only invented-verdict negatives; forged-mode and forged-derived-flag negatives; complete-suite status controls | Not a Return Desk function | Scientific cases require a nonempty allowed projection; the two official-state cases require exact `{}` and no scientific verdict | Missing scientific projection or an invented status-case verdict is `candidate_failed`. A scorer that changes the frozen mode or derived flags is `trial_invalid_controller`; status-only classification cannot rescue a scientific contradiction. |
| Text artifacts are strict UTF-8 and model-authored compiler inputs cannot launder string escapes into controls | Compiler v9 retains compiler v8's explicit `report_body_lines` contract with zero Unicode `Cc` characters per element, inserts LF itself, recursively rejects controls in return-template strings and generated JSON keys/values, normalizes textual media types across every artifact role, and validates their bytes before hashing or transport; Python and browser Return Desk scans remain independent downstream defenses | `\alpha`, `\beta`, `\forall`, `\nabla`, `\theta`, `\rho`, and `\varphi` escape-collision negatives; request/source controls; mixed-case, parameterized, JavaScript, YAML, and suffixed-JSON media; Unicode/plain-text and literal-backslash positives; NUL, VT, FF, ESC, DEL, invalid UTF-8, and Return Desk regressions | Blocks invalid encoding or prohibited controls | Build every model-authored text artifact from explicit control-free lines, prefer Unicode/plain-text math, never use ordinary string literals for LaTeX commands, and regenerate rather than delete or guess on failure | Compiler v9 preserves the producer-side control-byte repair and the failed v7 artifact as negative evidence. |
| Request and report have distinct roles; source bytes are not laundered through request/report roles | Schema roles plus Return Desk role checks and duplicate-digest defense | Role mismatch, alias, duplicate digest, and source-role negatives | Blocks request/report alias, wrong roles, duplicate filenames/digests | If request bytes equal target bytes, emit no envelope until byte-distinct request and source artifacts exist | Prior oracle allowed an impossible duplicate-digest alias. Repaired fail-closed without relaxing duplicate-role negatives. |
| Receipt artifacts, records, activities, claims, and gates are closed bidirectionally | Return Desk receipt indexing, uniqueness, role, authority, activity, and scope checks | Receipt reuse, authority, scope, and unverified negatives | Recomputes receipt effectiveness and blocks inconsistent scope | Receipt evidence must bind its own artifact, same claims/gates, and cited run | Strong structural enforcement. Receipt contents still cannot authenticate the external event they describe. |
| Claim/evidence and gate/evidence references agree in both directions | Return Desk binding comparison and missing-reference checks | Claim/evidence and gate/evidence asymmetry mutations | Blocks mismatches and derives gate state from all bound evidence | All references must resolve both ways | Deterministically enforced structurally. Semantic relevance remains a human/model judgment. |
| Every verified role-`evidence` proof artifact is an output of a cited execution, not merely accompanied by an unrelated output | Python and browser Return Desk support-output closure | Decoy-output and missing-proof-output negatives | Blocks `RETURN_EVIDENCE_SUPPORT_OUTPUT_MISMATCH` | Proof evidence must bind claim, execution output, and pass gate | A real loophole was found and closed in both runtimes; existing negative criteria were not weakened. |
| Fatal gates preserve negative evidence, derive `conflict` when pass and fail coexist, and admission is conjunctive | Return Desk gate derivation and admission projection | Conflict, concealed evidence, secondary gate, and summary mutations | Recomputes states and blocks submitted mismatches | No score rescue; pass requires claim-bound verified evidence and checks | Deterministically enforced for gates declared in the envelope. Code cannot discover a required gate omitted from the envelope without an external profile contract. |
| Passing gates have no open obligation; nonpassing gates retain at least one; obligations bind the same claim/gate/evidence scope | Compiler v9 validates obligation closure before rendering, hashing, or transport; Python and browser Return Desk independently repeat the checks | Compiler and Return Desk pass-with-obligation, nonpass-without-obligation, bidirectional binding, summary projection, claim-owner scope, evidence-scope, and refuted-claim remediation positives and negatives | Blocks projection or scope mismatches | Pass gate `obligation_ids=[]`; every `fail`, `unrun`, or `conflict` gate retains at least one scoped obligation; a refuted claim keeps a workflow disposition obligation without reopening scientific uncertainty | The controller-valid C004 obligation-topology failure is preserved. Compiler v9 separates refutation closure from admission disposition and fails before emitting invalid artifacts. Lifecycle history is still outside a single return. |
| Exactly eight execution activities are present and each `ran` non-model activity has bound inputs, exact tool/version, and verified output or admissible receipt | The compiler owns the canonical roster/order and transaction-known rows before serialization; schema plus Return Desk independently recompute execution effectiveness | Missing/unknown/duplicate activity, critical `not_applicable`, contradictory unexecuted output, blank identity, missing input/output/receipt, and unsupported-run tests | Blocks inadequate records, including any raw envelope that bypasses the compiler | Section 7 and terminal execution status must copy the compiler-owned ledger | Deterministically enforced for declared structured fields. It does not prove that an external event actually occurred. |
| Data Analysis writes/hashes are `ran` and bind one dedicated execution-output artifact | The embedded compiler creates exactly one `chatgpt_data_analysis_output.txt` from the captured session runtime and final metadata of all earlier-frozen outputs, then serializes the return | Compiler transaction, runtime projection, hash/size, topology, and Return Desk checks | Requires an adequate `ran` record and verified bound output | `model_reasoning`: request+target+six Knowledge inputs, evidence+report outputs, no receipt. Data Analysis: same inputs, same outputs plus the runtime ledger, no receipt | Consolidated repair removes the receipt alias and incomplete input/output rosters without weakening execution honesty. |
| Any schema/semantic validation claim has a bound validator output containing validator version, schema/input hashes, and result | No dedicated producer-claim parser in v0.1 | Candidate contract/oracle tests only | No dedicated validator activity exists in audit-return v0.1 | Otherwise say unvalidated/not_run and grant no dependent pass | Model/oracle/manual gate only. Return Desk validates the envelope itself but cannot prove a producer's separate validator claim without a structured transcript. |
| Unexecuted BSC checker, external proof tool, and empirical test use `not_run`, not `not_applicable` | Python and browser Return Desk invariant alignment | Canonical positive changed to `not_run`; misuse negatives added | Blocks `RETURN_EXECUTION_NOT_APPLICABLE_MISUSED` | Exact status rule is fatal | Cross-layer drift in the prior canonical positive fixture was removed. |
| The session-reported runtime has one captured source and deterministic projections | The executed embedded compiler reads its own full `sys.version` once; the model-authored spec cannot supply or override it. The compiler writes that value in one bound execution-output artifact and projects it into structured `execution.version`; the report references that artifact instead of copying the literal | Known-runtime projection, single-source, spec-override, invented-literal, and contradiction negatives | Compares the bound execution output with the structured field; a report reference satisfies the visible binding without triple literal replication | Describe the value as session-reported unless independently authenticated; never invent or manually copy a runtime string | The original R01 runtime contradiction remains `candidate_failed`; the repair removes triple model-authored replication rather than weakening execution honesty. |
| The exact target, all six canonical Knowledge files, and every generated output reach Return Desk | Evaluation controller validates the complete expected roster before replay and supplies that roster to Return Desk | Complete/incomplete source roster, omitted target, omitted Knowledge, omitted output, and parser-mutation tests | Does not score an incomplete or mutated replay | Exactly seven base source rows remain required in the candidate report | Missing required inputs or parser mutation produces `trial_invalid_controller` before candidate scoring. |
| Visible report sections and machine projections do not strengthen or contradict one another | Return Desk compares bound projections; frozen evaluation scoring checks required sections, references, verdict, and execution honesty | Manual rubric, 39-case frozen regression protocol, report/reference, and runtime projection tests | Recomputes machine gates/admission but does not parse every report claim | Complete sections 1-9 and 10 when required; summary cannot strengthen; report references the bound runtime output | Scientific prose remains subject to the frozen rubric and human inspection. |
| Candidate, controller, checker, tests, fixtures, expectations, rubric, and runtime implementations remain byte-frozen during counted evaluation | Frozen-candidate manifest plus before/after/final verification | Freeze-manifest generation, exact-membership, and mutation tests | Not a Return Desk function | Two uncounted preflights precede freeze; all 39 counted cases then use the same freeze | Any counted mutation stops the suite. Three explicitly authorized consolidated root-cause repairs have been consumed; the replacement candidate must refreeze and restart both preflights and all 39 counted cases, and a fourth complete candidate failure stops publication fail-closed. |

## Preserved R01 forensic result

The profile-`2a2a4248` Case 27 run remains a negative result. Its mathematics
passed, but execution and representation consistency failed. The visible
response, returned metadata, and exported payload did not carry one consistent
runtime/byte identity. That substantive contradiction is `candidate_failed`;
later controller or transport findings do not turn it into a pass.

The original Return Desk replay supplied five generated outputs but omitted the
exact case target and all six canonical Knowledge files. It is therefore
`trial_invalid_controller` and is neither a candidate pass nor a candidate
failure. A later complete-roster replay narrowed the remaining Return Desk
finding without erasing the independently observed candidate contradiction.

The first Base64 export of `audit_report.md` decoded to 5,422 bytes and did not
match the declared 11,527-byte payload. A clean retry decoded to 11,527 bytes
with SHA-256
`ce9bc53431459a903de78b75b3c08af3550150731c17c0ff2fed38ab4b6bd5cf`.
Downstream decoding was exact, including detection of an aligned omitted
quartet, `ZW5k` (`end`). Original download-button bytes were unavailable, so
their identity is `transport_identity_unresolved`; browser/download corruption
was not established.
